Algoritmo "Multiplicação"

Var

         n, x, resp : inteiro

Inicio

              escreva ("Qual número será a base da multiplicação? ")
              leia (n)
              
              para x de 1 ate 10 faca

                   resp <- n * x
                   
                   escreval (x, " x ", n,  " = ", resp)
                   
              fimpara


Fimalgoritmo